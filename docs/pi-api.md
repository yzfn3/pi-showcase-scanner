# Pi API documentation moved

The earlier proposed API is replaced by the implemented mock node. See [pi_node_api.md](pi_node_api.md) for current endpoints and examples. Scan creation uses POST /api/v1/scan/start, status uses GET /api/v1/scan/status/{scan_id}, and the default port is 8000.
